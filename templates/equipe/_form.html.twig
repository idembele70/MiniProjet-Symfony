{{ form_start(form) }}
<div class="container">
    <div class="row">
        <div class="col-xs-3 mx-auto">
            <div class="form-group">
                <label for="{{form.nomEquipe.vars.id}}">Nom de l'equipe : </label>
                <input type="text" class="form-control" name="{{form.nomEquipe.vars.full_name}}"
                    id="{{form.nomEquipe.vars.id}}" value="{{form.nomEquipe.vars.value}}">
            </div>
            <div class="form-group">
                <label for="{{form.entraineur.vars.id}}">Nom de l'entraineur : </label>
            {% if form.entraineur.vars.value %}
                <input type="text" class="form-control" name="{{form.entraineur.vars.full_name}}"
                    id="{{form.entraineur.vars.id}}">
            </div>
            {% else %}
                <input type="text" class="form-control d-none" name="{{form.entraineur.vars.full_name}}"
                    id="{{form.entraineur.vars.id}}">
                🚫
                {% endif %}
            </div>
            {% do form.entraineur.setRendered %}
            {% do form.nomEquipe.setRendered %}
            <div class="d-flex" >
                <button class="btn btn-success mx-auto">{{ button_label|default('Sauvegarder') }}</button>{{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
{% extends 'base.html.twig' %}

{% block title %}Edit Entraineur{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-3">
            <h1>Modifier les informations de l'entraineur</h1>
            {{ include('entraineur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            
            <a class="btn btn-primary d-block mx-auto w-25" href="{{ path('entraineur_index') }}">Afficher les entraineurs</a>
            
            {{ include('entraineur/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Entraineur{% endblock %}

{% block body %}
    <h1>Entraineur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entraineur.id }}</td>
            </tr>
            <tr>
                <th>NomEntraineur</th>
                <td>{{ entraineur.nomEntraineur }}</td>
            </tr>
            <tr>
                <th>PrenomEntraineur</th>
                <td>{{ entraineur.prenomEntraineur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entraineur_index') }}">back to list</a>

    <a href="{{ path('entraineur_edit', {'id': entraineur.id}) }}">edit</a>

    {{ include('entraineur/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Entraineur{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-3 mx-auto bg-dark text-white text-center">
            <h1>Entraineur</h1>
            <div class="card bg-dark" style="width: 18rem;">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item bg-dark">Id : {{ entraineur.id }}</li>
                    <li class="list-group-item bg-dark">Préom de l'entraineur : {{ entraineur.prenomEntraineur }}</li>
                    <li class="list-group-item bg-dark">Nom de l'entraineur : {{ entraineur.nomEntraineur }}</li>
                    <li class="list-group-item bg-dark">Equipe : {{entraineur.getIdEquipe() }} </li>
                    <a class="btn btn-primary my-2 w-75 mx-auto" href="{{ path('entraineur_index') }}">Afficher les entraineur</a>
                    <a class="btn btn-info my-2 w-50 mx-auto" href="{{ path('entraineur_edit', {'id': entraineur.id}) }}">Modifier</a>
                    {{ include('entraineur/_delete_form.html.twig') }}
                </ul>
            </div>
        </div>
    </div>
</div>

{% endblock %}